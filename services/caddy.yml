services:
  caddy:
    image: caddy:latest
    container_name: caddy
    ports:
      - "80:80"     # publish HTTP on the LXC host
      - "443:443"   # publish HTTPS on the LXC host
    volumes:
      - ../configs/caddy/Caddyfile:/etc/caddy/Caddyfile:ro  # Config file
      - ${DATA:?DATA is not set}/caddy/data:/data           # Caddy's cert store (internal CA, certs, OCSP cache)
      - ${DATA:?DATA is not set}/caddy/config:/config       # Admin API state (optional but nice to persist)
      - ../web:/web:ro                                      # Local websites to serve
    restart: unless-stopped
    # Ceiling so one service cannot starve the other twelve on a 2 GB
    # host. Measured ~34 MB.
    mem_limit: 128m
    healthcheck:
      test: ["CMD", "caddy", "validate", "--config", "/etc/caddy/Caddyfile"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: ${HEALTHCHECK_TIMEOUT:-5s}
      retries: ${HEALTHCHECK_RETRIES:-3}
      start_period: ${HEALTHCHECK_START_PERIOD:-20s}